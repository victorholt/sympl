// Example of a hello world program that will run
// in a console environment.

func hello(a, b)
{
    printl(a + b + "!");
}

var greeting = "Hello ";
var name = "John" + " " + "Doe";

hello(greeting, name);
