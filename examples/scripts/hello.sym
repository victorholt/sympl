profiler_start("hello");

// Example of a hello world program that will run
// in a console environment.

func hello(a, b)
{
    return a + " " + b + "!";
}

var greeting = "Hello";
var name = "John" + " " + "Doe";

var r = hello(greeting, name);

printl(r);

print_memory();

profiler_stop("hello");
