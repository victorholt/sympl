// Examples for sympl.

var scripts_dir = "../../examples/scripts/";

var example = 4;

// Hello world.
if (example == 1) {
    import(scripts_dir + "hello.sym");
}

// Math example.
if (example == 2) {
    import(scripts_dir + "math.sym");
}

// Recursive example.
if (example == 3) {
    import(scripts_dir + "rec.sym");
}

// Arrays example.
if (example == 4) {
    import(scripts_dir + "arrays.sym");
}

