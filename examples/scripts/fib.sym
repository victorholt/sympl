// Simple fibonacci method.
// ** BROKEN CURRENTLY **

func add(a, b) : int {
    return a + b;
}

func fib(n) : int {
    printl(n);
    if (n < 1) {
        return add(1, add(3, 2));

        // doesn't work...
        // return fib(n + 1);
    }
    return n;
}

var x = fib(0);
printl(x);
