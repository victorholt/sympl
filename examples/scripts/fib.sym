// Simple fibonacci method.

func fib(previous, current) : int {
    if (current < 12) {
        print(current);
        print(", ");
        return fib(current, previous + current);
    }
    return current;
}

var last = fib(0, 1);
printl(last);

var x = print_memory("test");
