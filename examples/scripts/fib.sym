// Simple fibonacci method.
// ** BROKEN CURRENTLY **

func add(a, b) : int {
    printl(a);
    return a + b;
}

//func fib(n) : int {
//    printl(n);
//    if (n < 2) {
//        return add(n, add(3, n));

        // doesn't work...
        //return fib(n + 1);
//    }
//    return n;
//}

//var x = fib(3);

var x = add(2, 3);
printl(x);
