// Simple math example.
profiler_start("math");

func add(a, b) {
    return a + b;
}

var x = 0;
while (x < 150000) {
    x = x + 1;// add(x, 1);
}

printl(x);

print_memory();

profiler_stop("math");
