// Simple math example.
profiler_start("math");

func add(a, b) {
    return a + b;
}

var x = 0;
while (x < 150000) {
    x = add(x, 1);
}

printl(x);

//print_memory_map();

print_memory();

profiler_stop("math");
