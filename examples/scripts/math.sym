// Simple math example.

func add(a, b) : int {
    return a + b;
}

func up(p) : int {
    if (p < 5050) {
        return up(p + 1);
    }
    return p;
}

var x = up(0);
printl(x, true);

//print_vm_refs();
