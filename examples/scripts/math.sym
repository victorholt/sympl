// Simple math example.

func add(a, b) : int {
    return a + b;
}

func up(p) : void {
    if (p < 1050) {
        return up(p + 1);
    }
    return p;
}

//var y = add(1, 3);
//var x = add(2, add(add(9, y), 2));

//printl(y);

var n = up(0);
printl(n, true);

//print_vm_refs();
